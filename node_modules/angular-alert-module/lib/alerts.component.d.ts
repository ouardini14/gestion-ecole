import { OnInit } from '@angular/core';
import { AlertsService } from './alerts.service';
export interface Message {
    message: string;
    type: string;
    [key: string]: string;
}
export declare class AlertsComponent implements OnInit {
    private alerts;
    messages: Object[];
    timeout: any;
    constructor(alerts: AlertsService);
    ngOnInit(): void;
    removeMessage(message: Object): void;
}
